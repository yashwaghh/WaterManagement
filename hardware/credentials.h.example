// Copy this file to credentials.h and fill in your actual values.
// credentials.h is gitignored and must NEVER be committed to source control.

// 🔹 WiFi
const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// 🔹 Firebase Realtime Database URL (no trailing slash)
const char* firebaseHost = "https://YOUR_PROJECT_ID-default-rtdb.REGION.firebasedatabase.app";
